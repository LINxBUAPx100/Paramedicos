node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local

# Artefacto reproducible de la plantilla oficial (se regenera con gen:plantilla)
scripts/plantilla-oficial.json

# Material de trabajo: renders de PDF y volcados intermedios. Son artefactos
# temporales y, en el caso del plan oficial, material de terceros: no se
# publican en un repositorio que sirve el sitio.
tmp/

# Registros del emulador de Firebase (npm run test:rules)
firebase-debug.log
firestore-debug.log
ui-debug.log
pubsub-debug.log
.cache/
