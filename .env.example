# Config de Firebase (cliente). Copia este archivo a `.env` y rellena los valores.
# Se obtienen en: consola Firebase → ⚙ Configuración del proyecto → Tus apps →
# app web (</>) → objeto `firebaseConfig`.
#
# Estos valores son PÚBLICOS (config de cliente: acaban en el bundle) y la
# seguridad real la dan las Security Rules. Aun así son OBLIGATORIOS: el build
# se detiene si falta alguno, para no publicar una app incapaz de conectarse.
# En GitHub Actions llegan como secrets del repositorio (ver deploy.yml).

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=ptem-a304f.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=ptem-a304f
VITE_FIREBASE_STORAGE_BUCKET=ptem-a304f.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=944566588634
VITE_FIREBASE_APP_ID=

# --- App Check (OPCIONAL) -------------------------------------------------
# Clave de sitio de reCAPTCHA v3, en: consola Firebase → App Check → Apps →
# app web → reCAPTCHA v3. Mientras esté vacía, App Check no se inicializa y la
# app funciona igual; al rellenarla, la CSP abre sola los orígenes que
# reCAPTCHA necesita (ver vite.config.js).
#
# Es lo que impide que alguien copie la apiKey del bundle y use tu proyecto
# desde un script propio. Actívala DESPUÉS de restringir la clave por
# referente HTTP en Google Cloud.
VITE_APPCHECK_SITE_KEY=

# --- Subida de archivos (opcional) -----------------------------------------
# Firebase Storage exige plan Blaze y en este proyecto NO está activado: el
# bucket no existe y cualquier subida devuelve 404. Mientras esto esté vacío, el
# editor pide PEGAR EL ENLACE del material (Drive, Dropbox…), que es como se
# sirve todo el contenido de la plataforma hoy.
#
# El día que se active Blaze: crear el bucket, publicar storage.rules
#   npx firebase-tools@15 deploy --only storage --project ptem-a304f
# y poner esta variable a 1. El código de subida ya está escrito y sus reglas
# ya se prueban en CI contra el emulador.
VITE_STORAGE_ACTIVO=

# --- EMULADOR LOCAL (solo desarrollo) --------------------------------------
# Pon esto a 1 para que `npm run dev` hable con el emulador de Firebase en vez
# de con el proyecto real. Sirve para probar pantallas que exigen sesión (panel,
# consola, tutoriales) sin crear gente en la academia de verdad.
#
#   1) npm run emu                      (en otra terminal; necesita Java 21)
#   2) FIRESTORE_EMULATOR_HOST=127.0.0.1:8080 \
#      FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099 \
#      GCLOUD_PROJECT=ptem-a304f npm run seed:usuarios -- --apply
#   3) VITE_FIREBASE_EMULADOR=1 aquí abajo, y reinicia `npm run dev`
#
# Con el emulador APAGADO y esta variable en 1, la app no conecta con nada:
# coméntala para volver a producción. En el build de producción se ignora
# siempre (la rama se elimina en compilación).
VITE_FIREBASE_EMULADOR=
